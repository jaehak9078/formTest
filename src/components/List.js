import React from "react";
import Header from "../components/Header";
import { Link } from "react-router-dom";

const List = () => {
  return (
    <div>
      <Header />
      <div>목록페이지입니다.</div>
    </div>
  );
};

export default List;
